mxcp: 1
project: mxcp-docker
profile: prod
dbt:
  enabled: true
sql_tools:
  enabled: true
profiles:
  prod:
    duckdb:
      path: data/mxcp.duckdb
    audit:
      enabled: true
      path: logs/audit.jsonl